@page "/records/create"

@attribute [Authorize]

@using Library.Client.Components.BorrowRecords

@inject NavigationManager Navigation

<BorrowRecordForm OnSaved="OnSaved" />

@code {
    private void OnSaved() => Navigation.NavigateTo("/records");
}
